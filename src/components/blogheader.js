import React from "react"

const BlogHeader = ({ imgSrc }) => (
  <div class="text-white rounded  text-center">
    <img src={imgSrc} width="50%" />
    <div class="col-md-6 px-0"></div>
  </div>
)

export default BlogHeader
